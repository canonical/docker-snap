project: docker
# Headroom for slow, emulated cross-architecture boots. Failures are bounded
# well below this by the per-attempt timeout and FATAL in the allocate script,
# so this only ever caps a genuinely slow (but progressing) boot.
kill-timeout: 45m
backends:
  # LXD is currently not supported as a backend, because we need to set lxc instance
  # config to make docker work in an lxc container,
  # see https://ubuntu.com/tutorials/how-to-run-docker-inside-lxd-containers#2-create-lxd-container

  #lxd:
  #  systems:
  #    - ubuntu-24.04

  garden:
    type: adhoc

    # Applies to the allocate/discard scripts below. The project-level
    # kill-timeout does NOT cascade here -- without this, spread's 15-minute
    # default kills a cold emulated allocation mid-provision (an ubuntu-core
    # provision boot alone takes ~12 minutes under TCG). Must stay above the
    # allocate watchdog (30m) plus the ssh poll (10m).
    kill-timeout: 45m

    allocate: source spread/scripts/allocate.sh
    discard: source spread/scripts/discard.sh
    systems:
      # Want to add more systems? Look here for a list of available systems: https://gitlab.com/zygoon/image-garden/-/tree/main/data
      # or look here for an exhaustive example: https://gitlab.com/zygoon/image-garden/-/blob/main/spread-demo/spread.yaml
      # Note: ubuntu core images are prepared via this script: https://gitlab.com/zygoon/image-garden/-/blob/main/mk/050-ubuntu-core.mk
      - ubuntu-core-22.amd64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_AMD64)
      - ubuntu-core-22.arm64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_ARM64)
      - ubuntu-cloud-22.04.amd64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_AMD64)
      - ubuntu-cloud-22.04.arm64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_ARM64)

      - ubuntu-core-24.amd64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_AMD64)
      - ubuntu-core-24.arm64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_ARM64)
      - ubuntu-cloud-24.04.amd64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_AMD64)
      - ubuntu-cloud-24.04.arm64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_ARM64)
      - ubuntu-cloud-24.04.riscv64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_RISCV64)
      # No ubuntu-core systems for s390x/ppc64el: Ubuntu Core is not published
      # for those architectures.
      - ubuntu-cloud-24.04.s390x:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_S390X)
      - ubuntu-cloud-24.04.ppc64el:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_PPC64EL)

      - ubuntu-core-26.amd64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_AMD64)
      - ubuntu-core-26.arm64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_ARM64)
      - ubuntu-cloud-26.04.amd64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_AMD64)
      - ubuntu-cloud-26.04.arm64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_ARM64)
      - ubuntu-cloud-26.04.riscv64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_RISCV64)
      - ubuntu-cloud-26.04.s390x:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_S390X)
      - ubuntu-cloud-26.04.ppc64el:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_PPC64EL)

      # Development series: early warning for the next release's surprises
      # (image layout, boot stack, snapd behaviour) before they reach an LTS.
      - ubuntu-cloud-26.10.amd64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_AMD64)
      - ubuntu-cloud-26.10.arm64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_ARM64)
      - ubuntu-cloud-26.10.riscv64:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_RISCV64)
      - ubuntu-cloud-26.10.s390x:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_S390X)
      - ubuntu-cloud-26.10.ppc64el:
          username: ubuntu
          password: ubuntu
          environment:
            SNAP_FILE: $(HOST:echo $SNAP_FILE_PPC64EL)

path: /tmp/docker
exclude:
  - .git
  - "*.qcow2"
  - "*.iso"
  - "*.img"
  - "*.log"
  - "*.run"
  - "*.pid"
  - ".image-garden/*"

suites:
  spread/main/:
    summary: Full-system tests for the docker snap

environment:
  SCRIPTS_PATH: $SPREAD_PATH/spread/scripts
  SNAP_CHANNEL: $(HOST:echo $SNAP_CHANNEL) # The Snap Store channel containing the version of docker to test

prepare: |
  # Invoked in each system before running any test
  # Learn more about preparing and restoring: https://github.com/canonical/spread?tab=readme-ov-file#preparing

  . "$SCRIPTS_PATH/prepare.sh"
